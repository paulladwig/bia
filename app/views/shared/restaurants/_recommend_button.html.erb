<% selected = Selection.find_by(user: current_user, restaurant: restaurant) %>
<%recommended = selected.recommended if !selected.nil? %>
<button class ="biabutton<%=" biabutton-active" if recommended%> size-48-pc font-size-awesome" <%='disabled' if recommended%> id="open-modal"> <i class="fas fa-bullhorn"></i> </button>
